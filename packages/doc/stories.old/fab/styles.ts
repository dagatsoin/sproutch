import { Styles } from '@sproutch/ui'

export const root = Styles.createViewStyle({
  flex: 1,
  justifyContent: 'center'
})

export const button = Styles.createViewStyle({
  flex: 0
})

export const fadeContainer = Styles.createViewStyle({
  marginTop: 50,
  height: 50
})