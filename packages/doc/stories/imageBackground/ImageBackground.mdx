import { Canvas, Controls, Description, Meta, Source } from '@storybook/blocks';

import * as ImageBackground from './ImageBackground.stories';

<Meta of={ImageBackground} />

## Border image

<Canvas/>

<Description/>

### Props

<Controls/>

### Code example

On the following example, you can see a view with an image as a border.

The layout breakdown is:
- a top container with with constrained size
- a BorderImage children
The center of the container is a background color

<Source of={ImageBackground.Default} />;