import { storiesOf } from '@storybook/react'
import * as React from 'react'
import { Styles, Text } from 'reactxp'

import { Paper } from '../paper'
import { View } from '../view'

storiesOf('Sproutch', module).add('Paper', () => (
  <>
    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={0}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>0dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={1}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>1dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={2}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>2dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={3}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>3dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={4}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>4dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={5}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>5dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={6}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>6dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={7}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>7dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={8}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>8dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={9}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>9dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={10}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>10dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={11}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>11dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={12}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>12dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={13}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>13dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={14}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>14dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={15}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>15dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={16}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>16dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={17}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>17dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={18}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>18dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={19}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>19dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={20}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>20dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={21}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>21dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={22}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>22dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={23}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>23dp</Text>
      </View>
    </Paper>

    <Paper
      style={Styles.createViewStyle({
        margin: 15,
        width: 120,
        height: 60,
      })}
      elevation={24}
    >
      <View
        style={Styles.createViewStyle({
          flex: 1,
          alignItems: 'center',
          justifyContent: 'center',
        })}
      >
        <Text>24dp</Text>
      </View>
    </Paper>
  </>
))
